/**
 * Global preloader stub — the home page manages its own Three.js preloader
 * via landing.js. Non-home pages no longer show a preloader.
 */
export function initPreloader() {
    // intentionally empty — see landing.js → runPreloader()
}
